<div class="container px-5 pt-5">

  <div class="headers d-flex flex-row justify-content-between mb-0">
    <div>
      <%= link_to list_path(@list), class: "fs-4", title: "Back to list" do %>
        <i class="fa-solid fa-arrow-left"></i>
      <% end %>
    </div>

    <div>
      <h1 class="mb-0"><%= @movie.title %></h1>
    </div>

    <div></div>
  </div>

</div>

<div class="container mt-2 mb-5 d-flex flex-column align-items-center w-50">

  <div class="headings">
    <h1 class="mx-3"><%=  %></h1>
  </div>
  <div>
    <img src="<%= @movie.poster_url %>" class="figure-img img-fluid rounded-3 m-5" style="height: 500px; width: 300px;">
  </div>
  <div>
    <p><%= @movie.overview %></p>
  </div>
</div>
